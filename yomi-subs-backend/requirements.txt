# Yomi-Subs v2 — Python backend dependencies
# Install with: pip install -r requirements.txt
#
# Note: faster-whisper pulls in PyTorch-free CTranslate2. If you want CUDA
# acceleration, install the CUDA build of CTranslate2 separately:
#   pip install ctranslate2 --extra-index-url https://download.pytorch.org/whl/cu118

websockets>=12.0
faster-whisper>=1.0.0
av>=12.0.0
numpy>=1.24.0
